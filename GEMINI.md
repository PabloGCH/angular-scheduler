- Take into account the project uses Angular 21.
- Use tailwind scss for styling.
- All style files should be scss and not css.
- Avoid adding type suffixes to filenames (e.g., avoid .service.ts, .guard.ts, .component.ts) as recommended by the Angular style guide.
- Prefer using the inject function over injecting constructor parameters
